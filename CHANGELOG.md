# Packet Lens 版本记录

本文件记录正式发布版本的重要变化。日期采用北京时间对应的发布日期。

## v0.7.0 - 2026-09-03

这是 Packet Lens 从单一网络工具扩展为本地运维工作台的首个版本。

### 新增

- 新增按工作域组织的首页与导航：网络分析、资产与连接、安全中心、规划与诊断、知识与 AI。
- 新增服务器台账、SSH/RDP 连接配置、Windows DPAPI 凭据库和 SSH 内嵌终端。
- 新增固定白名单服务器巡检、定时任务、脱敏快照和结果差异。
- 新增 DNS、Ping、路由、TCP、TLS、HTTP 和可选 SSH 的一键诊断。
- 新增事件中心，汇总巡检、诊断和安全分析产生的异常。
- 新增 IP/VLAN 规划，支持 `/8` 至 `/30`、自动/手动掩码、本地 VLSM 校验、保存和重新计算。
- 新增 AI 规划草案和规划审核；AI 结果必须通过本地规划器校验。
- 新增本地知识库，支持常见文本格式和 PDF、SHA-256 去重、中文全文搜索与来源引用。
- 新增 OpenAI 兼容和 Ollama 多提供商配置。
- 新增本机访问令牌、CSRF/Origin 校验和 SSH WebSocket 短期票据。

### 改进

- 首页重构为状态优先的运维工作台，显示事件、服务器、巡检、诊断、规划和 AI 状态。
- 默认主题调整为浅色专业运维风格，保留深色主题切换。
- 机房内多个机柜支持一行或两行展开、分行横向滚动与自定义分配。
- AI 配置统一保留在右上角设置中，移除首页重复入口。
- 旧路由自动映射到新的工作域路由，尽量保持已有书签可用。
- AI Key 从旧版明文配置迁移到 DPAPI 加密凭据库。
- `monitor.db` 和机柜数据迁移到 `%APPDATA%\PacketLens`。

### 移除

- 完整移除 Kubernetes 页面、接口、数据模型、Python 依赖和后续规划入口。
- 升级时清理旧 Kubeconfig 凭据、K8s 事件、诊断记录、知识元数据和集群表。

### 安全说明

- Web 服务仅允许绑定本机回环地址。
- 凭据 API 不返回秘密原文，日志和结构化结果统一脱敏。
- 云端 AI 调用需要先展示并确认实际上下文，默认遮蔽内网 IP。
- SSH 终端正文不持久化；自动巡检不接受任意 Shell 命令。

### 验证

- 后端自动测试：79 项通过。
- React 生产构建通过。
- Windows x64 单文件 EXE 构建和启动冒烟测试通过。
- `PacketLens.exe` SHA-256：`5DD51857CA00A9F54F99A6AAC7D91A7762392F52D820A6A07A7D6C363751F77C`。

## v0.6.0 - 2026-09-01

### 新增

- 合并网络探测与安全工作台。
- 支持 Ping、HTTP 和 TCP 批量探测。
- 新增暴露面发现和网络设备配置审计。
- 新增定时探测监控、运行历史和结果差异。
- 新增离线安全报告与可选 AI 增强分析。
- 新增机柜设备批量复制流程。

### 修复

- 修复新增设备未正确关联当前所选机柜的问题。

## v0.3.0 - 2026-09-01

### 新增

- 本地离线诊断报告成为默认分析方式。
- 抓包分析与网络探测支持导出 Markdown 报告。
- AI 调整为可选增强能力，没有 AI 配置时仍可完成主要分析流程。
- 提供 Windows 10/11 x64 单文件发行版。

### 其他

- 项目改用 GPL-3.0 许可证发布。
